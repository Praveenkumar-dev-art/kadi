1.) Copy the place where you want to actually run the system to the command line interfaces
2.) Clone the repository of mine using,(git clone https://github.com/Praveenkumar-dev-art/kadi.git)
3.)Navigate into your newly created project folder: by using this line( cd kadi)
4.) Install the needed dependencies:( npm install)
5.) (npm run dev) use this to run
6.) Make sure to use your own API keyusing.env file that is , createyourown.env file,in Which place this exact words (VITE_GEMINI_API_KEY=abcd(Use your own API key "Keys from Gemini"))

conclution: Don't worry about whether the app will run or not because it has been perfectly managed with error boundaries to handle error and make sure to use your own API key using
If needed install and save it in your pc, This version of this application works fine in Windows as of test.
